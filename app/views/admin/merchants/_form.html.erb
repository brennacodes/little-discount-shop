<%= render 'admin/nav' %>

<%= form_with(model: [:admin, @merchant], local: true) do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.label :status %>
  <%= f.select :status, ["disabled", "enabled"] %>

  <%= f.submit "Submit" %>
<% end %>